#include "Car.h"
/*
void Car::draw(sf::RenderTarget &target, sf::RenderStates states) const {

	// draw the car body
	target.draw(*sprite, states);

	// draw the wheels
	for (int i = 0; i < 4; i++) {
		//target.draw(*wheels[i], states);
	}
}*/

//void Car::updatePhysics(float dt) {
//	
//}